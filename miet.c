//read the values of a,b and c. add them and after addition check the sum is between 100 to 200 or not and print different message.

#include<stdio.h>
int main()
{
    int a,b,c;
    scanf("the values are %d %d %d",&a,&b,&c);
    int sum=a+b+c;
    if ("100>=sum>=200")
    {
     printf("the sum is between 100 and 200")
     {
        printf("it is in between 100 to 200");
     }

     }
    

